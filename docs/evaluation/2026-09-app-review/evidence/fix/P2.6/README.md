# P2.6

The whole of Phase 2 ships one regression test,
`tests/backend/deploy_07_update_rollback_and_backup.py`, because the four
deployment scripts are one file cluster and a finding in one is usually
visible from another. Its before and after logs are in
[`../P2.1`](../P2.1): **11 of 45 before, 46 of 46 after**.

The findings closed at this step are named in `docs/TRACKER.md` and each
carries its own `resolution:` line in
`../../findings.md`.
